import {start as startRouterBasicsExample}
  from './router_basics/RouterBasicsExample';

if (document.location.search.match(/progress=router_basics/)) {
  startRouterBasicsExample();
} else {
  startRouterBasicsExample();
}
